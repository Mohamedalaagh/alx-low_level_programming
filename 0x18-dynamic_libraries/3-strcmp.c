#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings.
 * @s1: string 1 to compare.
 * @s2: string 2 to compare.
 * Return: 0.
 */
int _strcmp(char *s1, char *s2)
{
    int res;

    res = strcmp(s1, s2);
    return (res);
}

